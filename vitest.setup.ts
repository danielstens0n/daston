// Extends vitest's `expect` with @testing-library/jest-dom matchers
// (toHaveValue, toBeInTheDocument, etc.) and augments the TS types.
// Referenced from vitest.config.ts#test.setupFiles.
import '@testing-library/jest-dom/vitest';
