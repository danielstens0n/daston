/** Keep in sync with `src/web/tokens.css` `--color-accent`. */
export const ACCENT_HEX = '#0162ff';

/** Default button border; pairs with `ACCENT_HEX` in preview defaults. */
export const ACCENT_BORDER_HEX = '#0050D0';

/** Keep in sync with `--color-selection` / `--color-chrome-focus` in tokens.css. */
export const CHROME_SELECTION_HEX = '#0162ff';
